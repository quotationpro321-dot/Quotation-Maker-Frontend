/** Cookie (SSR) + localStorage (client) — keep in sync for reload persistence. */
export const SIDEBAR_STATE_COOKIE_NAME = "sidebar_state";
export const SIDEBAR_STATE_STORAGE_KEY = "dashboard_sidebar_open";
export const SIDEBAR_STATE_MAX_AGE = 60 * 60 * 24 * 7;
